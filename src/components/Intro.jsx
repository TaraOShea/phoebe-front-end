
import React from "react";
import { SanityDocument } from "@sanity/client";

function Headeroverlay({ intro }: { intro: SanityDocument[] }) {
 return (
    <div className="intro">
        intro
    </div>
 );
}

export default Headeroverlay;